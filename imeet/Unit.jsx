// Unit component - represents a single calendar unit
Unit = React.createClass({
  propTypes: {
    unit: React.PropTypes.object.isRequired,
  },

  render() {

    // Give unit a different className for different status,
    // so that we can style them nicely in CSS


    return (
    <td  ></td>
    );
  }
});